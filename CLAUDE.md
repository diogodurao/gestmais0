# Git Rules
- NEVER commit or push changes without explicit permission
- NEVER deploy to production automatically
- Always ask before any git operations (commit, push, merge)
- Show me the changes first and wait for my approval